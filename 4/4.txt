In a binary tree, each node has two child pointers, totaling 2N for N nodes. 
There are N-1 child nodes, excluding the root node, which is not a child of any node.
Therefore there are N+1 null pointer, caculated as 2N - (N-1)
